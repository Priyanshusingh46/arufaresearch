import CfgErrors from "./config-errors";
/**
 * Validates the config, throwing a BuilderError if invalid.
 * @param config
 */
export declare function validateConfig(config: any): void;
export declare function getValidationErrors(config: any): CfgErrors;
